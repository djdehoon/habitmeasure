"use client";

// Placeholder for Fase 2: timer session CRUD and lifecycle methods.
export function useTimerSessions() {
  return {
    listSessions: async () => ({ data: [], error: null as string | null }),
  };
}
